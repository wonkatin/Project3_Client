export default function CreateTrip(props) {
  return(

    // Needs Form for user to create new trip
    // Will render on top of Trip Detail page
    // Will disappear when user clicks submit

      <div>
          Hello from CreateTrip
      </div>
  )
}